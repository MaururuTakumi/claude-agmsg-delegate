Proceed with one real delegation under `workspace_read` using
`Read,Glob,Grep`. Preserve every named exclusion in the task. The user's
explicit Fable request and private-workspace approval are sufficient, so there
is no second confirmation. Run the wrapper once and evaluate its concrete
result.
