From the actual target project, propose `<target-project-name>-team / codex`,
display both values, and wait for confirmation before creating the team with
the provided `join.sh` procedure. Never create the team silently.

After joining, ask for delivery separately. Offer only `1) turn` (recommended;
empty input or Enter selects it) and `2) off` for manual inbox checks.
Never select or recommend `monitor` or `both`, even if the raw installer displays them.
Apply the confirmed choice with `delivery.sh`, then resume the installed Skill
`--dry-run` from the target project without invoking Fable or Sonnet.
