Prerequisites are Python 3.9+, Codex, Claude Code, and agmsg. First git clone the repository, run make test and ./install.sh --dry-run, then ./install.sh. Install the offline Skill before resolving agmsg or other runtime dependencies. From the target project run doctor. If it reports missing or outdated agmsg, stop and get explicit approval before npx agmsg, team join, or delivery selection. Restart Codex after setup.
