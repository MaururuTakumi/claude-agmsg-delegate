Stop and get explicit approval before running `npx agmsg`: agmsg installation,
team joining, and delivery configuration change local agent routing.

After approval, run `npx agmsg`, inspect the current project with the provided
`whoami.sh` from the target project where delegation will actually be used. Do
not enroll the temporary repository copy merely to pass verification. Ask for
the team and agent names before using `join.sh`, and ask the user to choose
`turn` or `off` before using `delivery.sh`. Never edit the agmsg database,
config, or team files directly and never guess routing settings. Once one
target-project identity and team are confirmed, resume the same installed-Skill
`--dry-run`. Do not invoke Fable or Sonnet during setup or verification.
