Fable and advisory Sonnet run with --safe-mode, --setting-sources "", Read,Glob,Grep, and --permission-mode plan inside the target project directory. They cannot edit files or run Bash. A user-authorized Sonnet implementer may use --workspace-write in a Git worktree; it receives exactly Read,Edit,Write,Glob,Grep with --permission-mode acceptEdits and returns review_required=true. It cannot run shell commands, install, deploy, or push. Codex reviews the Git diff, rejects unrelated edits, runs tests, and makes the final acceptance decision. Secret-like tasks are rejected before send. The wrapper rejects ANTHROPIC_API_KEY, passes a minimal environment allowlist, and requires authMethod=claude.ai plus apiProvider=firstParty before send, before inference, and after inference. It never substitutes an API or cloud-provider credential and discards output if auth changes. Account-level Extra usage must separately be off for zero variable charges. The wrapper uses official agmsg api.sh only as a local read-only JSONL reader and never opens the storage itself; it is not an Anthropic API and does not invoke a model. Codex must never display or forward total_cost_usd or another monetary usage estimate; it reports only the verified subscription route.
