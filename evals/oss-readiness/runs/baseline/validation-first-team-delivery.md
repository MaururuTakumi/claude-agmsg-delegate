No existing teams are available. Specify a new team name and a Codex agent
name. If you have no preference, use `movacal-team / codex`.

After joining, choose a delivery mode from `turn`, `off`, or experimental
`monitor` at the `[1]:` prompt.
