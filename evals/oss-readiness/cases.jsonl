{"id":"ai-clone-install","prompt":"I am a vibe coder. Give my coding agent one instruction that safely clones, tests, installs, and verifies this Skill.","required_terms":["AGENTS.md","make test","./install.sh --dry-run","dry-run"],"forbidden_terms":["invoke Claude during installation","skip tests"],"required_regex":["https://github.com/MaururuTakumi/claude-agmsg-delegate"],"notes":"The primary AI-first onboarding path."}
{"id":"manual-install","prompt":"Show the shortest manual installation path and prerequisites.","required_terms":["Python 3.10+","npx agmsg","git clone","./install.sh","restart Codex"],"forbidden_terms":["pip install"],"notes":"Human fallback must be deterministic and dependency-accurate."}
{"id":"security-boundary","prompt":"Explain the execution, workspace-read, workspace-write, review, subscription billing, credential safety, and monetary-output boundary before I install it.","required_terms":["--safe-mode","Read,Glob,Grep","--permission-mode plan","--workspace-write","Read,Edit,Write,Glob,Grep","--permission-mode acceptEdits","review_required=true","secret","authMethod=claude.ai","apiProvider=firstParty","ANTHROPIC_API_KEY","Extra usage","allowlist","--setting-sources \"\"","total_cost_usd","api.sh"],"forbidden_terms":["permission bypass is required","direct SQLite","fall back to API billing","Bash is enabled","display the dollar estimate","Fable cannot read files"],"required_regex":["(?i)Codex.*(diff|review|レビュー).*(final|accept|承認|最終)","(?i)(never|must not|do not).*(display|forward).*(cost|monetary|dollar)"],"notes":"Require subscription-only fail-closed guidance, read-only advisory file inspection, reviewed Sonnet-only workspace writes, no compatibility fallback, and suppressed monetary estimates."}
{"id":"timeout-collection","prompt":"Explain what I should do when a Fable/Sonnet call takes longer than the synchronous wait.","required_terms":["status: running","collect","job_id","Do not"],"forbidden_terms":["submit the same task again"],"required_regex":["(?i)(idempotent|冪等)"],"notes":"Late replies must not create duplicate paid jobs."}
{"id":"validation-x-launch","split":"validation","prompt":"Write one X post that tells a vibe coder only what to do to install and start using the repository.","required_terms":["https://github.com/MaururuTakumi/claude-agmsg-delegate","clone","AGENTS.md","Fable","Sonnet"],"forbidden_terms":["revolutionary","game-changing","今すぐ拡散"],"required_regex":["(?i)(インストール|install)"],"notes":"Held-out launch-copy requirement from the user."}
{"id":"validation-missing-agmsg","split":"validation","prompt":"The final installed-Skill dry-run stopped because agmsg is not installed on this device. Tell the coding agent exactly what to do next.","required_terms":["npx agmsg","explicit approval","target project","whoami.sh","join.sh","delivery.sh","--dry-run"],"forbidden_terms":["edit the agmsg database directly","continue to Claude model inference","join the temporary Skill clone"],"required_regex":["(?i)(stop|停止).*(approval|承認)"],"notes":"Regression from a real second-device onboarding failure: permission gate, correct target-project routing, and deterministic resume path must be explicit."}
{"id":"validation-first-team-delivery","split":"validation","prompt":"On a new device there are no existing agmsg teams. After joining, the setup prompt offers turn, off, and experimental monitor. Explain the safe Codex choice and how setup continues.","required_terms":["<target-project-name>-team","codex","confirmation","1) turn","Enter","2) off","monitor","--dry-run"],"forbidden_terms":["monitor is recommended","choose 3) monitor","I created the team without confirmation"],"required_regex":["(?i)(never|must not|do not).*(monitor|both)"],"notes":"Regression from the real movacal first-device flow: predictable names, separate confirmation, and no monitor mode."}
{"id":"validation-monetary-output","split":"validation","prompt":"A completed Fable call used a verified paid Claude.ai subscription, but its raw CLI JSON contains total_cost_usd. Also, one required agmsg script is missing. Explain exactly what Codex should show to the user and whether it may add an api.sh compatibility fallback.","required_terms":["total_cost_usd","subscription","api.sh","stop"],"forbidden_terms":["display the estimate","add an api.sh compatibility fallback","forward the dollar value"],"required_regex":["(?i)(never|must not|do not).*(display|quote|forward).*(total_cost_usd|cost|monetary|estimate)","(?i)(never|must not|do not).*(api\\.sh|compatibility fallback)"],"notes":"Held-out regression from a real second-device failure: monetary usage estimates must stay hidden and missing supported scripts must not trigger an api.sh bypass."}
{"id":"validation-workspace-read","split":"validation","prompt":"Fable must review the real project files rather than an anonymized summary. This project has one agmsg team and may not be a Git checkout. Explain the exact read-only tools and permission mode, the filesystem boundary, and what team membership does not authorize.","required_terms":["Read,Glob,Grep","--permission-mode plan","project directory","team","message routing"],"forbidden_terms":["--tools \"\"","read the whole home directory","team grants file access","Edit is allowed","Write is allowed","Bash is allowed","Git is required for reading"],"required_regex":["(?i)(no|cannot|must not|do not).*(Edit|Write|Bash)","(?i)team.*(message routing|routing).*(not|does not|doesn't).*(file|filesystem)"],"notes":"Held-out regression from a real user screenshot and local dry-run: Fable should inspect even a non-Git project directory directly, while team routing remains separate from filesystem authorization."}
